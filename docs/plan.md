[X] P1. (2026-09-07) 现在我想增加一个功能，因为mineru输出的内容有非常好的格式化结构并且识别的准确率挺高的。可以增加如下功能 1. 一个开关来控制让一个专门配置的校验的支持图像的大模型对输出的每一张和对应的内容进行核对查看是否有问题并且给出修改意见，因为毕竟庞大这个功能可以放最后或者下一次实现，先提上日程，并且默认关闭这个ai检验功能。 2. 这次主要实现的功能：支持输出latex版本，可以有两种档位，(2)最低档，对文档中所有可以结构化的图片，如函数图像，立体结构图或者，各种能够用latex绘制的图像等等可以用latex绘制的比如tikz或者svg等，使用tikz或者其他方式进行绘制，然后由我们的软件对其自动编译并查看编译结构和是否报错，栅格化反馈给模型进行核对，然后保留这个pdf，最终嵌入在markdown中是矢量图最清晰。在这个工作流程中，和之前是使用ai对图像的内容进行解释，首先一个ai对要处理的图像进行分类，这次是需要一个专门的作图ai的模型名（在配置文件中可以对每个后面支持的模型进行单独配置其调用方法和baseurl），首先需要其进行判定该图像是否是可以矢量化的而非人物/字母序号/等等非结构化的图像内容，进行标记，因为有的是比如题目的号码，但是由于其艺术样式导致mineru将其裁切为图像，而不是真正的需要图像的内容是有艺术样式的文本，需要进行标记。然后后续再根据不同的标记进行处理，比如说文本的就使用之前的ai然后嵌入进原始的markdown文本流中就行了和之前一样，但是嵌入的时候这个档位下就不需要 <--->那个image标识和前面的 [AI]标识还有 [IMG_TYPE 什么的了，而是只提取真正的内容嵌入其中。对于确定是图像但是无结构类型的就原封不动的保留（配置文件中给定一个选项是否插入该图像的解释内容，默认不是默认就是插入图像的链接，如果插入图像的解释也不需要 <--->的注释内容而是 [Image]( 内容 ) 接内容就行，这种是用来给ai看的全部给嵌入的是可直接读的解释文本）；对于可以结构化的通过启动另一个ai的对话窗口，写好对应的提示词，让其写tikz的内容，然后可以输出并选择编译和核对结果，如果他确定了就可以提交（这里也是如果选择嵌入图像解释的话就嵌入的是这个tikz的代码块而不是编译之后的pdf的链接）。对于会话，每个ai会话都提供可以读前后上下文的工具和维护那个上下文，之前应该就这一个工具并且输出有mmdc的话自动检验（用latex这个的话就不用mmdc了不让他输出mermaid图了），这个ai会话的管理还需要分离工具调用的功能，因为后续还有比如编译latex，并查看结果就是自动返回一个png图像的结果让ai看看效果，还有选择最终输出吧，是最终确认提交，相当于是这些工具的可分离附加，方便不同会话的使用，然后就是上下文的维护，默认窗口在128K，可以在设置中进行更改，比如256K，64K什么的，然后还有一个自动压缩的功能，当会话达到上下文限制之后自动对整个会话中内容进行压缩使用ai来除去一些非关键的思考或者工具调用和之前的很多草稿内容，保留重要的关键的让下一次继续使用。 (1)档位是我们最后是要构建一个latex的全文本而非markdown，这次不局限于某些结构图片， 我们还需要一个ai专门对整本书或者整个文档的样式和结构进行分析并提供工具比如查看不同章节的那个原始的图片分析样式和排版结构和形式，可以对图像进行裁剪比如放大的查看某个具体区域等等功能，还有查看该文件输出的那个md（原始的包含很多图像链接的）对应的解析内容（这个比较准确比看起来的），还可以查看扣出来的这些每个图像的内容。就是这样他需要根据这些信息给出一个可以用的cls和对应的使用手册和案例，这里需要对使用手册和案例做好结构化输出，保证是我们可以处理的，并且要求详细完整，并且保证构建的案例样式和原版的书是一样的（这里会话的上下文可以单独设置比如256K在配置文件中），就是和前面一样那个会话管理器，工具，上下文压缩都要有的。然后单独的图片和(2)档位一样上面的一样处理流程处理完获得全部的tikz或者原图或者转文本之后的。（如果有保留原始图像的需要在日志中输出一下警告吧因为这种情况一般不多）。然后后续处理之后拿着这些所有的使用方法，可以对全书进行章节的划分（这里可以单独用一个专门的会话，提供全书的grep搜索功能或者其他的类似工具或者context7，可以提供这种检索功能，让该ai无需读取全文的情况下将在尽可能少的读取前提下，对这个md，划分之后通过一些工具比如bash来操作这个文件吧，提供一个最小化的bash环境吧，虚拟的里面文件系统只有这一个文件，然后划分之后可以操作比如行号分割为多个文件，尽可能让每个文件小一点但是是一个章节最少），查看那个mineru_output中有没有可以利用的结构信息。然后就可以并发的将所有的按照之前的cls和手册转换为latex，对于每个小文件，如果需要横跨文件了解信息比如可能有引用或者样式等等，这些文件应该是对所有的这些都是只读的，虽然每个人只处理他们自己的。最终再进行汇总和核对和原始样式是否一致，可以通过再次调整cls和批量替换总体的内容来更加完善最终的样式。然后最终形成一个具有多文件的那个.tex的结构，一个tex不可能写完所有的，就是我们得维护一个类似虚拟的文件目录一样，可以直接借助当前文件夹中某个文件夹作为虚拟的底座，他们都被系统限制在只能在这里面操作。然后最终需要编译成功为pdf，需要有良好的一个工作区干净的工作区环境，和构建单独的.tex文件（方便ai读取所有需要把所有的再合并为一个.tex不需要编译这个）。大概就是这个功能，可以通过这么多复杂的工序和拆解将一本书可以批量的转为latex并且保证结果的稳定性。规划一下并实现吧。

[ ][ ] P2. 增加翻译的功能，可以设定目标语言，对于img2txt和latex的功能都可以进行这个设定选项，可以进行完整的翻译，在转换的过程中将语言进行转换。对img2text可能需要多添加一个步骤，但是对于latex应该只需要在转换章节的地方更新提示词就可以。
[ ][ ] P3. 将我们的其中的核心工具可以独立为一个核心性的组件库，将这套工具变成一个可以低成本复用的工具组装。方便创建会话，注册工具，加载提示词，控制预算，成本，会话存储，沙箱等功能作为独立的核心部分。然后其他的方法可以对核心层进行编排和使用。然后通过编排，并发，流水，断点对底层核心的操作。从而解耦和更加高效的开发我们独立的应用功能，我们需要什么就用什么的go语言来实现就可以，无需考虑底层的会话问题。同时将UI界面也作为一个核心的预览部件，可以在UI上增加自己的独立内容，或者说其他工具之类的会被自动注册并且显示，提供一个良好的调试工具。
[X] P4. (2026-09-16 已实现：会话引擎双协议适配——models.*.<条目>.api 取 anthropic（Anthropic Messages API，/v1/messages，x-api-key/anthropic-version，thinking/tool_use/tool_result/图片块双向翻译）或 responses（OpenAI Responses API，/v1/responses，gpt-5/o 系，store:false 全量回传，加密思考链 reasoning.encrypted_content 回放）；cache_read 均计入缓存命中，两线路只走非流式。img2text 逐图分析客户端暂仍 OpenAI 兼容。)
[X] P5. 将前端ui独立出来开发，可以迁移到vue或者其他的现代的框架上。方便后续维护和开发进度，通过将现在的内容重构为各种组件和模块，同时可以支持移动端的响应式界面和高效性。最后构建和嵌入到assert中。并且提供可以独立注入的架构。
[X] P6. 在UI界面上对于点击图片进入的预览界面，增加可以用滚轮进行图片缩放的功能。
[X] P7. 增加当对话正在实时进行时的一个流式状态，就是输出内容的时候都是正在一个字一个字的出，还有工具调用也是。这边能更加好的实时的查看状态。
[X] P8. 还有之前的响应式界面和左侧的这个项目列表的显示方法，和文件夹图标可以仿照dsh做，那个比较好好看，左侧那个列表一个项目内的显示的可以方便查看一点，比如可以选择一个小方块一个小方块显示之类的。因为有时候矢量图和章节转换那边会有非常非常多的会话，这个是可以根据选择开的，类似文件夹中按照列表和图标显示那种，可以切换。内层的可以按照顺序递进，每个步骤的上面可以现在当前进行的状态比如正在运行或者已经结束，用颜色表示或者文字都可以。
[X] P9. 类似DSH那样每个工具调用在轨迹界面点击可以查看右边侧边栏里面的详细信息。
[X] P10. (2026-09-15 已实现：msg 行 h 内容哈希 + 会话哈希；工具卡尾部与轨迹展开区展示) 我希望增加一个给每个请求就是工具或者说Ai的输出内容，每轮都有一个内部的哈希值，还有每个会话也有哈希值。方便我们后期的维护和快速排查和定位以及传递那个轮次的会话出问题了。
[X] P11. CSS Vue 化：viewer.css 按归属拆进各组件 <style scoped>，全局只留 token/骨架/通用小件/灯箱（旧页冻结后不再需要整卷共享；sync-css/check-css 字节守卫退役）。
[X] P12. (2026-09-08 已实现)  有时候在img2text处理的时候会出现很多图片在img2text的普通模型无法处理的情况，他会经历3词mermaid的修复会话。有时候应该是会出现mermaid内容太过于复杂导致这边难以编辑和庞大的输出。可以考虑在mermaid失败之后这边对会话进行一个升级，将之前的错误的mermaid存储在比如submit.md的文件中，给他一个虚拟的工作区，和选择提交这个submit.md文件的功能类似latex的那边流程。让他能够充分的编辑这个内容，可以提供编译（就是mermaid的语法检查程序）。但是主要是因为这边是做img2text的所以要求不是很高，能编译通过就可以。这边提供写入编辑和grep工具还有查看原始图像工具吧？然后这边按照设置中的错误上限，累计编译错误三次（提交也要检查），进入备选模型（可以在设置中设定一个备选的更强大的兜底模型）。然后这边清理一下上下文。留任务和修改要求，以及这边出错的结果文件都保留，然后会话功能类似，这边设定一个检查次数上限（新的设置给6次吧默认）。然后让他来完成这个，最终提交。日志中记得有整理，还有分析的时候这边工具调用应该都在要算进去的。并且有一些特殊注意，多少个会话触发了这个后备的。
[X] P13. UI中增加一个功能，选择是否展示缩略图。（页签行右端「缩略图」开关：默认开、`localStorage showThumbs` 记忆、重载保持；关掉后看图调用下的缩略图预览行整行隐藏，对话流其余不变。）
[X] P14. (2026-09-16 本轮范围：灯箱触屏化——双指捏合缩放（双指中点为锚、按初始距离比绝对缩放）、双击 1×/2.2× 切换、拖动平移保留，右下角常驻 缩小/百分比/放大/1:1 控件，灯箱 touch-action:none 禁浏览器默认手势；折叠侧栏轨道美化——行 28px 纵向呼吸、hover 圆角高亮、当前会话 active 底、title 悬浮说明保留。更全面的移动端布局后续继续。)
[X] P15. (2026-09-16 已实现：checker 系统提示词加一句——可见回复不是交付物，不要写大段的核对比较分析，读、决定、提交即可。)
[ ][ ] P16. 增加一个估算价格的，最终总花费。可以用历史的记录这边单列一个专门算价格的，根据之前的各个模型，看和比如页数，字数，图片数量有一定的关系，根据输入的最终会估算出来多少和实际的相对比。还有可能进行多少轮会话，每个会话有某些固定的输入文本和图片，可以计算平均放大多少倍数的token然后计算出总的估算值根据之前的哪些关系。可以在处理一个内容之前就给出预计的花费（然后按照各部分估算的token和当前的设定单价去计算出花费金额）。可以增加一个选项，只计算不运行。
[ ][T] P17. 我给issue和plan增加了一个 [ ] 内容，第一个是Agent是否完成这个，后面是开发者审核和审批，完成是完成是 'T'，打回是 'F'。前面如果是正在做就是 '~' ，做完就是 X。
[X][ ] P18. 给img2text也进webui界面，可以通过web来查看img2text的当前进展状态，所有图片转换的会话之类的，要保存他们的会话记录，修复记录，还有一些会话升级之类的等等。都可以在UI界面中查看，并且要给UI界面进行一下增加这个可以切换latex和查看img2text的功能吧，这边分不同的板块，属于一个在T37之后的大web界面更新。
[ ][ ] P19. latex增加一个选项，可以保留复杂图像的原始图像嵌入，而不是重绘。这边可以在classify那边输出的时候增加一个复杂程度的判断，给几个档次，然后latex里面根据档次不同来筛选哪些不去用latex绘制。
[ ][ ] P20. 对于图片转latex的部分，可以考虑是否和style调换位置？因为书中有些图片或者内容具有相同的样式或者结构，或者某些复杂的样式和结构。可能在后续书中反反复复的出现，或许先写样式也可以？但是这样的话这个样式就需要考虑所有的图片是否符合？感觉或许先处理所有图片也可以？因为样式还可以根据处理的情况只做这个内容判断和风格迁移就可以了？这里需要讨论一下。