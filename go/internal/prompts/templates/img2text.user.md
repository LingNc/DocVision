The image to describe is at line {LINE}. Context: [{UP_START} to {DOWN_END}] ({UP}↑, {DOWN}↓).
```
{CONTEXT}
```
Understand image before output. If confused, call get_more_context(↑N, ↓M) — max per call: ↑{MAX_UP}, ↓{MAX_DOWN}.
