Redraw the attached image as TikZ.

{ORIGINAL_SIZE}
{VIEW_BUDGET}

Its surrounding document context (for correct labels/terminology):
```
{CONTEXT}
```

Begin by LOOKING at the attached image (view_image, crop/zoom as needed) — never draw before you have seen it. Then write the TikZ code to figure.tex with write_file and call compile {path: "figure.tex"}.
